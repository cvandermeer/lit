<% if @question.category_id == 1 %>
	<%= render 'form', question: @question %>
<% elsif @question.category_id == 2 %>
	<%= render 'choose_image_form', question: @question %>
<% else %>
	<%= render 'write_word_form', question: @question %>
<% end %>
