source 'https://rubygems.org'
gem 'rails', '4.2.1'

gem 'sass-rails', '~> 5.0'
# Sass-rails gem biedt officiële integratie voor Ruby on Rails projecten met de Sass stylesheet taal.

gem 'uglifier', '>= 1.3.0'
# Uglifier minimaliseert Javascript bestanden

gem 'coffee-rails', '~> 4.1.0'
# Coffee rails biedt integratie voor Ruby on Rails projecten met coffee script. Een taal die javascript minimaliseert.

gem 'jquery-rails'
# Jquery-rails biedt integratie voor Ruby on Rails projecten met jquery.

gem 'turbolinks'
# Turbolinks, maakt de links in de applicatie sneller

gem 'jbuilder', '~> 2.0'
# Bied de mogelijkheid voor het gebruiken van Json

gem 'sdoc', '~> 0.4.0', group: :doc
# Sdoc geeft informatie terug over bepaalde commands in de terminal

gem 'devise'
# Devise is een flexibele authenticatie oplossing voor Rails

gem 'nested_form'
# Bied de mogelijkheid voor het gebruiken van forms in forms

gem 'carrierwave'
gem 'rmagick'
# Deze gem bied de mogelijkheid tot het verwerken van de afbeeldingen die worden geupload

gem 'bourbon'
gem 'bitters'
# Basic styling of the application

gem "highcharts-rails", "~> 3.0.0"
# Gem voor het weergeven van de diagrammen

gem 'omniauth-facebook'
# Deze gem bied de mogelijkheid om in teloggen via facebook

group :development, :test do
  gem 'byebug'
  # Byebug geeft foutmelding weer voor het debuggen van de applicatie 

  gem 'web-console', '~> 2.0'
  # Web console is een debugging tool

  gem 'spring'
  # Spring maakt het mogelijk om een rails server snel op te starten

  gem 'sqlite3'
  # Sqlite is een light weight database

  gem 'minitest-reporters'
    # Visuele test output

  gem 'guard'
  gem 'mini_backtrace'
    # Zodra er een bestand is opgeslagen worden de tests uitgevoerd

  gem 'guard-minitest'
  # Filteren van de test output 
end

group :production do
  gem 'mysql2' 
    # Mysql is de online productie database

  gem 'capistrano', '~> 3.2.1'
  gem 'capistrano-rails', '~> 1.1'
    # Voor het online zetten van de server

  gem 'exception_notification'
  # Verstuurd notificatie als er een error voor komt in de applicatie
end
